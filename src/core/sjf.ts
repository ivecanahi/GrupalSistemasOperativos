import type { ProcessInput, SchedulingResult } from '../types/scheduling';

// Módulo 1 (motor): implementar SJF no apropiativo.
// Reglas: si dos procesos empatan en burstTime, desempatar por arrivalTime y luego por id.
export function runSJF(processes: ProcessInput[]): SchedulingResult {
  throw new Error('runSJF: pendiente de implementación');
}
