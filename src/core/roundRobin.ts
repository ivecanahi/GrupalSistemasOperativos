import type { ProcessInput, SchedulingResult } from '../types/scheduling';

// Módulo 1 (motor): implementar Round Robin con quantum fijo.
// El quantum se recibe por parámetro (definido en runtime por la UI), nunca hardcodeado.
export function runRoundRobin(processes: ProcessInput[], quantum: number): SchedulingResult {
  throw new Error('runRoundRobin: pendiente de implementación');
}
